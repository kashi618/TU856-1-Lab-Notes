#include <stdio.h>

int main () {
    char roomStatus = '0';
    int age = 20;
    float vatRate = 0.23;

    printf("The age is: %d\n", age); 
    printf("The room status is: %c\n", roomStatus);
    printf("The vat rate is: %f", vatRate);

    return 0;
}